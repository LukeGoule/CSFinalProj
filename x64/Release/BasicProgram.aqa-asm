; This is an example program that this assembly language emulator can execute.
; It will simply count to 200, then exit.

MOV R0, #0	; Clear R0
	
ADD R0, R0, #1	; Increment R0
CMP R0, #200	; Test R0 against the value 200
OUT R0			; Print R0
BNE 1		; Loop if not equal
	
HALT